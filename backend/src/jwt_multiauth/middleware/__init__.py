"""Reserved for Phase 9's Channels ``JWTAuthMiddlewareStack`` (``jwt_auth.py``). Empty package
marker only — importing this package itself never requires the ``channels`` extra; only
``jwt_multiauth.middleware.jwt_auth`` does, and that module guards its own import at the top
(see its docstring).
"""
